// Enums cannot be created inside a class and are a list created as below:
enum TextSizes { small, medium, larg }

enum OrderStatus { processing, shipped, deivered }

enum PaymentMethod {
  paypal,
  googlepay,
  applepay,
  mastercard,
  creaditcard,
  creditcard,
  razorpay,
  paytm
}
